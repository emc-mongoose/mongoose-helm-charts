apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  labels:
    app: {{ .Values.service.name }}
spec:
  clusterIP: "None"
  ports:
    - name: data
      port: 1099
      targetPort: 1099
  selector:
    name: {{ .Values.service.name }}